#define M 4
#include <stdio.h>
int fun (int a[][M])
{
int i,j,max=a[0][0];
for(i=0;i<2;i++)
for(j=0;j<M;j++)
if(max<a[i][j])
max=a[i][j];
return max;
}
void main()
{
  int arr[2][M]=5,8,4,6,44,1,-4,76};
  printf{"max=%d\n",fun(arr));
}
